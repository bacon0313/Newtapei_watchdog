{
  "version": "v1",
  "hooks": [
    {
      "name": "Python 語法檢查（存檔時）",
      "trigger": "PostFileSave",
      "description": "每次儲存 .py 檔時自動做 Python 語法檢查，及早發現錯誤。",
      "matcher": "\\.py$",
      "action": {
        "type": "command",
        "command": "python -c \"import sys,py_compile; f=sys.argv[-1]; py_compile.compile(f, doraise=True) if f.endswith('.py') else None\" 2>&1 || echo \"語法檢查未通過\""
      }
    }
  ]
}